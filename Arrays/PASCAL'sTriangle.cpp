#include <bits/stdc++.h>
using namespace std;

// vector<vector<int>> generate(int n)
void generate(int n)
{
    bool flag = false;
    vector<int> arrToPrint;
    int count =0, limit=0;
    
                   


}

int main()
{
    generate(7);
}